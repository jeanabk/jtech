let input,button,greeting;

function setup() {
  createCanvas(800, 600);
input = createInput();
input.position(20,65);
button = createButton("submit");
buttom.position(input.x + input.width,65);
button.mousePressed(greet);
greeting = createElment  
